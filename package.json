{
  "name": "qwen-chrome-extension",
  "version": "0.1.0",
  "private": true,
  "description": "A Chrome extension that answers questions about the current page and runs long autonomous browser tasks, powered by a local Qwen model served by Ollama.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types test/*.test.ts",
    "check": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "@types/jsdom": "^30.0.0",
    "@types/node": "^22.7.5",
    "esbuild": "^0.24.0",
    "jsdom": "^30.1.0",
    "typescript": "^5.6.3"
  }
}
